export interface Research {
    id: number;
    topic: string;
    summary: string;
    created_at: string;
}

export interface ResearchListItem {
    id: number;
    topic: string;
    created_at: string;
}